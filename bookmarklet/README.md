# スコア取得ブックマークレット

[chunisupport.net API](https://docs.chunisupport.net/api/#get-v1usersusernamerating) から自分のCHUNITHMスコアデータを取得し、
`index.html` が読み込める形式のJSONファイルとしてダウンロードするブックマークレットです。

## 中身

- [`fetch-score-data.js`](fetch-score-data.js) — 読みやすい形式のソース
- [`bookmarklet.txt`](bookmarklet.txt) — ブラウザのブックマークに登録する一行版(`javascript:...`)

## 登録方法

1. `bookmarklet.txt` の中身を全部コピーする
2. ブラウザで新しいブックマークを作成する
3. 名前を「CHUNITHMスコア取得」など好きな名前にする
4. URL欄にコピーした内容をそのまま貼り付けて保存する

## 使い方

1. **普通のWebページ(`http://`または`https://`のページ)を開いた状態で**、登録したブックマークをクリックして実行する
2. プロンプトでユーザーネームとAPIトークンを入力する
3. `chunithm_player_data.json` がダウンロードされる
4. そのファイルを `index.html` の「スコアデータを読込」またはドラッグ&ドロップで読み込む

## トラブルシューティング

**「Content Security Policy」や「connect-src」といったエラーが出て失敗する場合**

これは、ブックマークレットを実行したときに開いていたタブが `chrome://`(新しいタブページ、設定画面、ブックマークマネージャーなど)や `about:` から始まる**ブラウザの内部ページ**だったことが原因です。内部ページは非常に厳しいセキュリティ制限がかかっており、外部サイトへの通信がブロックされます。

**対処法**: 一度、普通のWebページ(例: このツールの `index.html` を開いたタブ、または任意のサイト)に移動してから、改めてブックマークをクリックしてください。

## 注意

このブックマークレットは [ChuniSupport利用規約](https://docs.chunisupport.net/legal/terms/) の適用を受けます。特に以下を守ってください。

- APIトークンは自分の chunisupport.net アカウントのものを使ってください。他人のトークンを入力・使用してはいけません。
- APIトークンは第三者に公開・共有・譲渡・貸与しないでください。このブックマークレットはトークンを実行のたびにプロンプトで入力させる方式で、コード中には保存していません。ブックマークのURL自体にトークンを埋め込んだり、入力したトークンをスクリーンショット等で公開しないよう注意してください。
- APIおよび取得データの利用は非営利目的に限られます。有料機能・広告掲載・データの販売や有償再配布などには使用できません。
- レート制限や公開設定など、APIドキュメントで定められた技術的制限を回避しないでください。
- ダウンロードされるJSONにはプレイヤーネームや個別のスコアが含まれます。公開の場に上げる際は取り扱いに注意してください。
